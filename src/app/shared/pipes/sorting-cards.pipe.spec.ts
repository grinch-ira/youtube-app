import { SortingCardsPipe } from './sorting-cards.pipe';

describe('SortingCardsPipe', () => {
  it('create an instance', () => {
    const pipe = new SortingCardsPipe();
    expect(pipe).toBeTruthy();
  });
});
